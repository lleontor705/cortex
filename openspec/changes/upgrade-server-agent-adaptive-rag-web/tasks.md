# Tasks: Scoped Adaptive RAG and Web Redesign

Budgets: Go ≤500 changed lines; TS/CSS ≤350. Only listed files are writable.

- [ ] **1.1 P0 vector hydration visibility** — Apply the canonical visibility predicate inside exact-project hydration and prove restricted/confidential/personal canaries. **Files:** `internal/store/postgres/authorized_operations.go`, `authorized_operations_test.go`, `agent_visibility_integration_test.go`. **Oracle:** `go test -count=1 ./internal/store/postgres`; tagged test `TestAgentVectorHydrationVisibility`. **Depends:** none. **Out:** ranking.
- [ ] **1.2 Score normalization** — Add signal-specific `[0,1]` normalization, deterministic ties and invalid-score rejection. **Files:** `internal/retrieval/agent_scores.go`, `agent_scores_test.go`. **Oracle:** `go test -count=1 ./internal/retrieval -run 'TestAgentScore'`. **Depends:** 1.1. **Out:** orchestration.
- [ ] **2.1 Semantic tracer slice** — Introduce the narrow domain retrieval result and deep server `ScopedAgentRetriever`; route semantic queries through authorized lexical+dense, RRF and MaxSim. **Files:** `internal/domain/agent/retrieval.go`, `service.go`, `service_test.go`, `internal/platform/server/agent.go`, `agent_retriever.go`, `agent_retriever_test.go`. **Oracle:** `go test -count=1 ./internal/domain/agent ./internal/platform/server -run 'Test.*Semantic'`. **Depends:** 1.2. **Out:** graphs/CRAG.
- [ ] **2.2 Multi-hop graph slice** — Reserve budgets before memory/AST acquisition; seed PPR from normalized hybrid hits; classify authorization versus availability; order canonically before truncation; add `Operations` and `requestOperations` delegation. **Files:** `internal/store/postgres/agent_graph.go`, `agent_graph_test.go`, `internal/platform/server/agent_retriever.go`, `agent_retriever_graph_test.go`, `http.go`, `request_operations.go`, `request_operations_test.go`. **Oracle:** `go test -count=1 ./internal/store/postgres ./internal/platform/server -run 'TestAgent.*Graph|TestScoped.*MultiHop|TestRequestOperationsAgentGraph'`. **Depends:** 2.1. **Out:** summaries/tagged infrastructure.
- [ ] **usarw-2.2i / 2.2i PostgreSQL 16 graph canary** — Through real `requestOperations` and context-bound `AuthorizedStore`, prove bounded deterministic retrieval, sibling-scope/visibility exclusion, and auth/availability classification. **Files:** `internal/platform/server/agent_graph_postgres_integration_test.go`. **Oracle:** `go test -count=1 -tags postgres_integration ./internal/platform/server -run '^TestAgentGraphProductionDelegationPG16$'`. **Depends:** 2.2 approved; manual orchestrator route. **Out:** product code/summaries.
- [ ] **2.3 Architectural summaries** — Derive/cache memory+AST community summaries under combined checksums and reject stale/non-ready snapshots. **Files:** `internal/platform/server/agent_summaries.go`, `agent_summaries_test.go`, `agent_retriever.go`. **Oracle:** `go test -count=1 ./internal/platform/server -run 'TestAgentArchitectural|TestAgentSummary'`. **Depends:** usarw-2.2i passed. **Out:** persistence/migrations.
- [ ] **2.4 CRAG correction** — Evaluate normalized evidence, perform one scope-preserving reformulation, then answer/degrade/abstain. **Files:** `internal/retrieval/agent_crag.go`, `agent_crag_test.go`, `internal/platform/server/agent_retriever.go`, `agent_retriever_crag_test.go`. **Oracle:** `go test -count=1 ./internal/retrieval ./internal/platform/server -run 'TestAgent.*CRAG|TestScoped.*Refine'`. **Depends:** 2.3. **Out:** external-Web fallback.
- [ ] **3.1 Safe transport metadata** — Extend canonical answer/stream trace and prove JSON/SSE parity and redaction. **Files:** `internal/domain/agent/types.go`, `service.go`, `internal/platform/server/agent_http.go`, `agent_http_test.go`, `agent_security_test.go`. **Oracle:** `go test -count=1 ./internal/domain/agent ./internal/platform/server -run 'TestAgent.*(Metadata|Parity|Redact)'`. **Depends:** 2.4.
- [ ] **4.1 Web trace foundation** — Add typed metadata, shared scope/trace/source components and reducer coverage. **Files:** `web/src/lib/api.ts`, `api.test.ts`, `agent-chat.ts`, `agent-chat.test.ts`, `web/src/components/knowledge/ScopeBar.tsx`, `RetrievalTrace.tsx`, `SourcePanel.tsx`. **Oracle:** `cd web && npm test`. **Depends:** 3.1.
- [ ] **4.2 Redesign `/agent`** — Build responsive chat/workbench layout, progressive states and accessible focus/cancel/retry behavior. **Files:** `web/src/app/agent/page.tsx`, `page.test.tsx`, `web/src/app/globals.css`. **Oracle:** `cd web && npm test -- src/app/agent/page.test.tsx && npm run build`. **Depends:** 4.1.
- [ ] **4.3 Redesign `/search`** — Use eligible project scope and transparent grouped memory/code states with accessible empty/degraded recovery. **Files:** `web/src/app/search/page.tsx`, `page.test.tsx`. **Oracle:** `cd web && npm test -- src/app/search/page.test.tsx && npm run build`. **Depends:** 4.1.
- [ ] **5.1 Isolation and release gate** — Add PostgreSQL 16 cross-scope E2E, API/Web parity, docs and rollback flag. **Files:** `internal/platform/server/e2e_postgres_integration_test.go`, `docs/SERVER.md`, `docs/ARCHITECTURE.md`, `web/src/components/AppShell.tsx`. **Oracle:** `go test -count=1 ./... && go test -count=1 -tags postgres_integration ./internal/store/postgres ./internal/platform/server && cd web && npm test && npm run build`. **Depends:** 4.2, 4.3.

Parallel group: `{4.2, 4.3}`. Critical path ends at `5.1`.
