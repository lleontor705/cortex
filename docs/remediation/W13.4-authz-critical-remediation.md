# W13 Critical Remediation

## Scope

Remediates the W13 critical findings through commit `15b3189`.

- Removed raw scoring, observation, health, backend, and transaction methods from
  the transport-visible `AuthorizedStore` method set.
- Added narrow authorized score access/update/set/access-record operations; each
  resolves the observation resource before enforcement.
- Added explicit tenant predicates to observation topic lookup, listing, counts,
  PostgreSQL search, and score/edge reads.
- Added reflection and SQL-predicate architecture tests.
- Updated tagged PostgreSQL fixtures to use a migration DSN for schema bootstrap
  and a real `NOSUPERUSER` application login for test traffic.

## Verification

Passing locally on 2026-07-29:

- `go test -count=1 -coverprofile "$TEMP/cortex-default-70final3.out" ./...`
  — PASS; **70.002188% unrounded** (9,598 / 13,711 statements).
- `go test -count=1 -tags "integration postgres_integration" -covermode=atomic -coverpkg=./... -coverprofile "$TEMP/cortex-tagged-final.out" ./...`
  — PASS; **72.2% tagged aggregate**.
- Tagged PostgreSQL used a real `NOSUPERUSER NOBYPASSRLS` application login and
  separate privileged migration DSN; no `SET ROLE` was used for application
  traffic.
- `go test -race -count=1 ./...` — PASS.
- `go vet ./...` — PASS.
- `go build ./...` — PASS.
- `CGO_ENABLED=0 go test -count=1 ./...; go build ./...` — PASS.
- `golangci-lint run ./...` — PASS (0 issues).
- Bounded fuzz targets for authz, PostgreSQL principal validation, and both
  identity JWT targets (`-fuzztime=2s`) — PASS.
- `go test -count=1 ./internal/platform/server -run 'Test(AuthorizedStoreHasNoRawRepositoryAccessors|ArchitectureMutationPins)'`
  and toolchain pin test — PASS.
- PostgreSQL 16 container was stopped and removed after verification.
